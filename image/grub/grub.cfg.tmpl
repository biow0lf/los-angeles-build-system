set timeout=3
set default=0

menuentry "Los Angeles Linux" {
	insmod ext2
	search --no-floppy --fs-uuid --set=root @ROOT_UUID@
	linux /boot/vmlinuz root=UUID=@ROOT_UUID@ rootfstype=ext4 console=ttyS0
	initrd /boot/initramfs
}
